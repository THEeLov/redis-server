[package]
name = "redis-server"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.3", features = ["poll", "event"] }
tracing = "0.1.44"
tracing-subscriber = { version = "0.3.23", features = ["env-filter"] }
